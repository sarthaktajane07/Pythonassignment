# Given a list of integers, prompt for a specific integer and count how many times it appears.

numbers = [1, 2, 3, 2, 4, 2, 5]  # Example list
target = int(input("Enter an integer to count: "))
print("Count:", numbers.count(target))
