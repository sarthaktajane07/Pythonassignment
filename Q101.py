## Import Math Module: Write a script that uses the math module to compute the square root, floor, and ceiling of a user-input number.
import math
num = float(input("Enter a number: "))
print(f"Square root: {math.sqrt(num)}")
print(f"Floor: {math.floor(num)}")
print(f"Ceiling: {math.ceil(num)}")